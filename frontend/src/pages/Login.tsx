import { Box } from '@chakra-ui/react';
import loginImage from '../images/login.png';

const Login = () => {
  return (
    <Box minH="100vh" backgroundSize="cover" backgroundImage={loginImage}>
      Login
    </Box>
  );
};

export default Login;
